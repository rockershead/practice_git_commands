## this repo is to practise scenarios where git commands like rebase will be used


## things practised:
1) git rebase -i....and then add a drop to commits to drop certain commits
2) git config pull.rebase true
   git pull origin main